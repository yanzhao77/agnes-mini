# Architecture

Agnes Mini follows a layered architecture:

1. Models Layer (src/models/): Pydantic data models
2. Providers Layer (src/providers/): API integration
3. Agents Layer (src/agents/): Agent abstractions
4. Internal Tools (src/internal/): Dev tools
5. CLI Layer (src/cli.py): Command-line interface
